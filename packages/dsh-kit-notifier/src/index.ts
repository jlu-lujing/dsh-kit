/** dsh-kit-notifier host plugin (placeholder). */

import type { Context } from '@deepseek-ai/cordis'

/** Cordis plugin name. */
export const name = 'dsh-kit-notifier'

export function apply(_ctx: Context): void {
  // TODO: desktop notification on session done / needs-attention events.
}
