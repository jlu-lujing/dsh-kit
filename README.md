# dsh-kit

DeepSeek Harness (DSH) 傻瓜式插件全家桶 —— 装一个包，所有功能开箱即用。

## 结构

```
dsh-kit/
├── packages/
│   └── (各功能插件，dsh- 前缀)
└── package.json          # workspace 根
```

## 开发

```sh
pnpm install
pnpm build
```
